# Native editor rewrite

This branch replaces the WebView-first wrapper with a native Android audiovisual editor while retaining the legacy wrapper as an optional compatibility mode during the transition.
